# treasury-cache

Pre-computed snapshots of the **TrueSight DAO off-chain treasury** and the **DAO members directory**, regenerated automatically. This repo is **write-by-automation, read-by-humans-and-AIs**.

**Do not hand-edit any JSON.** Each file is rebuilt by its own Apps Script publisher on event triggers + a safety-net cron. The `main` branch history is the audit trail.

## Canonical files

| File | Purpose | Raw URL | Publisher source |
|---|---|---|---|
| [`dao_offchain_treasury.json`](dao_offchain_treasury.json) | Inventory across every managed ledger; warehouse-manager breakdown | [raw](https://raw.githubusercontent.com/TrueSightDAO/treasury-cache/main/dao_offchain_treasury.json) | [`gas/treasury-cache-publisher/`](gas/treasury-cache-publisher/) (this repo) |
| [`dao_members.json`](dao_members.json) | Active contributors with public keys, voting weight, **email**, **roles** | [raw](https://raw.githubusercontent.com/TrueSightDAO/treasury-cache/main/dao_members.json) | `tokenomics/google_app_scripts/tdg_identity_management/dao_members_cache_publisher.gs` |
| [`SNAPSHOT.md`](SNAPSHOT.md) | Human sanity-check companion to the treasury JSON | — | regenerated by treasury publisher |

## Source of truth

The authoritative data lives in Google Sheets:

- **Main Ledger** (`1GE7PUq-UT6x2rBN-Q2ksogbWpgyuh2SaxJyG_uEK6PU`) — sheet `offchain asset location` (rows starting at row 5, columns A=currency, B=manager, C=amount, D=unit cost USD, E=total value).
- **Shipment Ledger Listing** tab — config for every managed AGL ledger (column A=name, column AB=resolved spreadsheet URL).
- **Every managed AGL ledger** (external spreadsheets per-shipment) — sheet `Balance`, rows starting at row 6, columns H=manager, I=quantity, J=asset.
- **Currencies** tab — canonical catalog (unit cost, unit weight, **Inventory Type** col P, **Unit format** col Q), joined by currency name; enriches aggregated `items` and each `managers[].items[]` row in the JSON.

If this JSON looks wrong, the Sheets are authoritative. Regenerate by calling the publisher webhook (see [`gas/treasury-cache-publisher/README.md`](gas/treasury-cache-publisher/README.md)) — do **not** edit the JSON directly; the next run will overwrite your change.

## `dao_offchain_treasury.json` schema (v3)

`schema_version` **3** adds optional **`inventory_type`** and **`unit_format`** on each aggregate `items[]` row and on each `managers[].items[]` line, copied from Main Ledger **Currencies** columns P and Q when the currency name matches. Older consumers may ignore these keys; they are `null` when the sheet cell is blank or the name is not in Currencies.

```json
{
  "generated_at": "2026-04-21T19:56:10Z",
  "source": "treasury-cache-publisher",
  "trigger": "movement",
  "schema_version": 3,

  "items": [
    {
      "currency": "Oscar Bahia Ceremonial Cacao 200g",
      "unit_weight_g": 200,
      "unit_cost_usd": 7.12,
      "inventory_type": "Cacao Bean",
      "unit_format": "Retail ready",
      "total_quantity": 68,
      "total_value_usd": 484.16,
      "ledgers": {
        "Main Ledger": 12,
        "AGL14": 40,
        "AGL15": 16
      }
    }
  ],

  "managers": [
    {
      "manager_name": "Matheus Reis",
      "manager_key": "Matheus%20Reis",
      "items": [
        {
          "currency": "[AGL14] Oscar Bahia Ceremonial Cacao 200g",
          "amount": 40,
          "ledger": "AGL14",
          "unit_weight_g": 200,
          "unit_cost_usd": 7.12,
          "inventory_type": "Cacao Bean",
          "unit_format": "Retail ready",
          "total_value_usd": 284.80
        }
      ]
    }
  ],

  "ledgers": [
    { "ledger_name": "AGL14", "ledger_url": "https://truesight.me/..." }
  ],

  "totals": {
    "item_types": 42,
    "total_units": 1234,
    "total_value_usd": 9876.54,
    "ledgers_processed": 15,
    "managers_count": 38
  }
}
```

### Changelog

- **v3 (2026-04-26)** — optional **`inventory_type`** and **`unit_format`** on `items[]` and `managers[].items[]`, from Main Ledger **Currencies** columns P and Q (matched by currency name). Additive; older clients may ignore.
- **v2 (2026-04-21)** — added `unit_weight_g` to `managers[].items[]` (needed by `shipping_planner.html` → `get_inventory` compat). `schema_version` bumped. Additive only; v1 consumers keep working.
- **v1 (2026-04-21)** — initial release.

### Field notes

- **`items[].ledgers`** is a map, not an array: each key is a ledger name (e.g. `"AGL14"`, or `"Main Ledger"` for direct offchain-asset-location rows), each value is the quantity held in that ledger. Sum of values equals `total_quantity`.
- **`items[].unit_cost_usd`** comes from the Main Ledger `Currencies` tab column B. It is `null` when unknown.
- **`items[].inventory_type`** / **`items[].unit_format`** (v3+) come from `Currencies` columns P and Q when the name matches; `null` if blank or unknown.
- **`managers[].items[].currency`** uses the same `[AGLn] <asset>` prefix convention the DApp renders, so consumers can keep using it verbatim.
- **`managers[].items[].ledger`** is the ledger name (e.g. `"AGL14"`) or `"Main Ledger"` for `offchain asset location` rows.
- **`trigger`** is one of `movement` (posted by the tokenomics movement processor), `cron` (30-minute safety-net), or `manual` (`?action=publish` force-refresh).

### Schema stability

`schema_version` is bumped on breaking changes and on material additive changes we want consumers to notice (e.g. v3 catalog dimensions). Consumers should check it on load and fail loudly only if they require a cap they do not support. Optional new keys may appear without breaking strict parsers that allow unknown properties.

## `dao_members.json` schema (v3)

Aggregated directory of active DAO contributors keyed by display name; one record per contributor with all of their active public keys collapsed onto a single entry. Source: `Contributors Digital Signatures` + `Contributors voting weight` + `Governors` tabs on the Main Ledger spreadsheet.

```json
{
  "generated_at": "2026-04-27T18:00:00.000Z",
  "source": "dao_members_cache_publisher",
  "trigger": "edgar_webhook",
  "schema_version": 3,
  "counts": {
    "contributors": 11,
    "active_public_keys": 42,
    "governors": 5,
    "contributors_with_email": 8
  },
  "dao_totals": {
    "voting_rights_circulated": "...",
    "total_assets": "...",
    "asset_per_circulated_voting_right": "...",
    "usd_provisions_for_cash_out": "..."
  },
  "unjoined_governor_names": ["agl15"],
  "contributors": [
    {
      "name": "Gary Teh",
      "email": "garyjob@agroverse.shop",
      "roles": ["governor", "member"],
      "voting_rights": 43710,
      "total_voting_power_pct": "0.00017085...",
      "public_keys": [
        {
          "public_key": "MIIBIjAN...",
          "status": "ACTIVE",
          "created_at": "2025-07-06T15:50:30Z",
          "last_active_at": "..."
        }
      ]
    }
  ]
}
```

### Field notes

- **`contributors[].email`** (v3+) — first non-empty `Contributor Email Address` (col F on `Contributors Digital Signatures`) seen across the contributor's ACTIVE rows. `null` for legacy contributors that haven't registered an email yet. Lower-cased.
- **`contributors[].roles`** (v3+) — string array; always includes `"member"`. Includes `"governor"` when the contributor's display name appears on the `Governors` tab (auto-derived 4×/year from the trailing 180-day contribution leaderboard, per the workbook's own formulas).
- **`contributors[].public_keys[]`** — an `ACTIVE`-only list. Inactive / revoked keys are not emitted.
- **`unjoined_governor_names[]`** (v3+) — names that appear on the `Governors` tab but did not match any contributor (typos, ledger codes like `AGL15`, or governors who haven't registered a signature yet). Surfaced loudly because silently dropping a governor name would mask config drift.
- **`dao_totals`** — DAO-wide aggregates fetched from the `assetVerify` web app at publish time. `null` if the probe call failed; consumers should fall back to direct GAS in that case.

### Consumers (members snapshot)

| Consumer | Path | Purpose |
|---|---|---|
| `dao_client/cache/contributors.py` | Python lib | Authoritative read-side cache for CLI tools (`dao_client.modules.*`) |
| `dapp/scripts/dao_members_cache.js` | DApp client | Loads on every page so signed-in identity / voting weight / TDG balance can render without GAS round-trips |
| **(planned)** `dapp/scripts/permissions.js` | DApp client | Resolves `(signed-in RSA) → contributor → roles` for governor-only UI gating |
| `dapp/create_signature.html` | DApp client | Validates a fresh signature by checking against the active-keys list |
| `governor_chatbot_service/app/governor_registry.py` | Python service | Loads governor list for the governor-chat access gate |

### Update cadence (members snapshot)

- **Edgar webhook** — every successful `[EMAIL VERIFICATION EVENT]` activation triggers a refresh.
- **Daily cron** — Apps Script time trigger at ~03:00 UTC; bounds worst-case staleness if Edgar's ping ever drops.
- **Manual** — `publishDaoMembersCacheNow()` from the Apps Script editor (governor / ops only).

### Changelog (members snapshot)

- **v3 (2026-04-27)** — added `email` and `roles` per contributor; added `counts.governors` / `counts.contributors_with_email`; added top-level `unjoined_governor_names[]`. Backs the dapp permission model + add-contributor dedup pre-flight. Additive only; older clients keep working.
- **v2 (2026-04 earlier)** — added voting weight + DAO totals.
- **v1** — initial release (name + active public keys only).

## Consumers (treasury snapshot)

- **DApp** (`dapp/report_inventory_movement.html`) — renders Warehouse Manager + Inventory Item dropdowns from this file instead of fanning out to the `tdg_inventory_management` GAS on every page load.
- **External AI / integrations** — treat the raw URL as a read-only public JSON feed. It is eventually consistent (staleness bounded by the 30-minute cron).

## Update cadence (treasury snapshot)

- **Event-driven** — every inventory movement processed by `tokenomics/google_app_scripts/tdg_inventory_management/process_movement_telegram_logs.gs` posts to the publisher webhook. Expected latency Sheets → cache: < 1 min.
- **Time-driven** — Apps Script time trigger on the publisher project, every 30 min. Bounds worst-case staleness if a movement-trigger ever fails.
- **Manual** — `GET <publisher-webhook>?action=publish&token=<TREASURY_CACHE_PUBLISH_SECRET>&trigger=manual` forces a regen.

## Related

- Publisher GAS source + runbook: [`gas/treasury-cache-publisher/`](gas/treasury-cache-publisher/)
- Sibling repo for store-facing inventory: [`agroverse-inventory`](https://github.com/TrueSightDAO/agroverse-inventory) (different purpose: SKU → count for the Agroverse store, not internal treasury).
