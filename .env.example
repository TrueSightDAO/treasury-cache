# treasury-cache local secrets — copy to .env and fill in real values.
# .env is gitignored; never commit the real secret.

# Shared secret for the treasury-cache-publisher GAS HTTP trigger.
# Source of truth: Apps Script editor → Project Settings → Script Properties
#   (script 1u4lVtGaO5GjpG0XQo7b8hc3QFAXcsrJKclBead2sDcgvatMdE6dm3mzx)
# Used to force a regen:
#   GET <publisher-webhook>/exec?action=publish&token=$TREASURY_CACHE_PUBLISH_SECRET&trigger=manual
TREASURY_CACHE_PUBLISH_SECRET=
